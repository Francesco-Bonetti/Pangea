// ============================================
// PANGEA TYPE SYSTEM — Barrel export
// Import from "@/types" for DTO types at module boundaries.
// Import from "@/lib/types" for legacy types with join fields (UI components).
// ============================================

export * from "./core";
export * from "./edge";
