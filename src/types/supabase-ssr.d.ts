// Intentionally empty — @supabase/ssr types are provided by the package itself.
// This file exists only to satisfy TypeScript in sandboxed environments where
// node_modules may reference stale paths.
export {};
